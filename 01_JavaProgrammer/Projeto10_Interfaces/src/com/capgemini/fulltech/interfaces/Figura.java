package com.capgemini.fulltech.interfaces;

public interface Figura {
	//todos os atributo definidos numa interface são públicos, estáticos e constantes(public static final)
	//todos os métodos de interfaces são públicos e abstratos
	double calcularArea();

}
